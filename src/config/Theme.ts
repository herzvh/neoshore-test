import { ThemeOptions } from "@mui/system"

const primary = {
    main: '#d319af',
    light: '#ffffff'
}

const secondary = {
    main: '#24252d',
    light: '#ffffff'
}


export const Theme: ThemeOptions = {
    palette: {
        primary,
        secondary
    }
}